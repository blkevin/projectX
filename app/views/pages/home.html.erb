<% if user_signed_in? %>
  <div class="banner" id="banner-logged">
    <h1>PERSPECTIV</h1>
    <h2> 👋 <%= current_user.first_name %></h2>
  </div>
  <div class="container">
    <div class="section">
      <div class="d-flex justify-content-between mb-3 headers-section">
        <h3>Les mentors à découvrir</h3>
        <%= link_to "Voir plus >", mentors_path, class:"link-purple"%>
      </div>
      <div class="scroll-horizontal">
        <%= render "shared/item_scroll_mentor", mentor: @mentors[1] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[2] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[3] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[4] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[5] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[6] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[7] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[8] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[9] %>
        <%= render "shared/item_scroll_mentor", mentor: @mentors[10] %>
      </div>
    </div>
    <div class="section">
      <div class="d-flex justify-content-between mb-3 headers-section">
        <h3>Notre sélection de vidéos pour toi</h3>
        <%= link_to "Voir plus >", mentors_path, class:"link-purple"%>
      </div>
      <!-- Slider main container -->
      <div class="swiper" data-controller="swiper">
        <!-- Additional required wrapper -->
        <div class="swiper-wrapper">
          <!-- Slides -->
          <% @contents.each do |content| %>
            <div class="swiper-slide">
              <%= render "shared/item_scroll_video", content: content %>
            </div>
          <% end %>
        </div>
        <!-- If we need pagination -->
        <div class="swiper-pagination"></div>
      </div>

      <div class="scroll-horizontal">
        <% @contents.each do |content| %>
          <div class="modal fade modal-video" id="content-modal-<%= content.id %>" tabindex="-1" aria-labelledby="content-modal-<%= content.id %>Label" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
              <div class="modal-content">
                <div class="modal-header">
                  <h1 class="modal-title fs-5" id="content-modal-<%= content.id %>Label">Modal title</h1>
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                  <%= video_tag(content.video.url, autoplay: false, controls: true, loop: false) %>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                  <button type="button" class="btn btn-primary">Save changes</button>
                </div>
              </div>
            </div>
          </div>

        <% end %>
      </div>
    </div>
  </div>

<% else %>
  <div class="banner">
    <h1>PERSPECTIV</h1>
    <h2>Trouve ta voie et éclaire ta perspective</h2>
    <%= link_to 'Créer un compte', new_user_registration_path, class: "btn-purple" %>
  </div>

  <div class="container">
    <div class="home-steps">
      <div class="step-home">
        <i class="fa-solid fa-user-plus"></i>
        <h5>Inscris-toi</h5>
        <p>Réponds à un quizz rapide pour cerner tes ambitions et préférences</p>
      </div>
      <div class="step-home">
        <i class="fa-solid fa-globe"></i>
        <h5>Découvre les profils des mentors</h5>
        <p>Accède à un panel de profils ciblés selon les résultats de ton quizz</p>
      </div>
      <div class="step-home">
        <i class="fa-solid fa-comments"></i>
        <h5>Discute et rencontre</h5>
        <p>Discute avec les mentors via notre chat et réserve de courts entretiens avec eux</p>
      </div>
    </div>
  </div>
<% end %>
